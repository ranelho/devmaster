# ========================================
# DevMaster - Variáveis de Ambiente
# ========================================
# IMPORTANTE: Copie este arquivo para .env e configure com seus valores reais
# O arquivo .env está no .gitignore e NÃO deve ser commitado

# ========================================
# AMBIENTE
# ========================================
SPRING_PROFILES_ACTIVE=develop
SERVER_PORT=8081
CONTEXT_PATH=/api

# ========================================
# APLICAÇÃO
# ========================================
APP_VERSION=1.0.0
APP_TIMEZONE=America/Sao_Paulo
APP_DEBUG=true

# ========================================
# BANCO DE DADOS
# ========================================
# Opção 1: Supabase (Cloud) - Configure suas próprias credenciais
# DATABASE_URL=jdbc:postgresql://your-supabase-host:5432/your-database
# DATABASE_USERNAME=your-username
# DATABASE_PASSWORD=your-password

# Opção 2: Local com Docker (padrão)
DATABASE_URL=jdbc:postgresql://localhost:5432/devmaster_dev
DATABASE_USERNAME=devmaster
DATABASE_PASSWORD=devmaster123

# Pool de Conexões
DATABASE_POOL_SIZE=10
DATABASE_MIN_IDLE=2
DATABASE_CONNECTION_TIMEOUT=30000
DATABASE_IDLE_TIMEOUT=600000
DATABASE_MAX_LIFETIME=1800000

# ========================================
# JPA/HIBERNATE
# ========================================
JPA_DDL_AUTO=update
JPA_SHOW_SQL=true

# ========================================
# LOGGING
# ========================================
LOG_LEVEL=DEBUG
LOG_LEVEL_WEB=DEBUG
LOG_LEVEL_SQL=DEBUG
LOG_LEVEL_SQL_PARAMS=TRACE

# ========================================
# SWAGGER/OPENAPI
# ========================================
SWAGGER_ENABLED=true

# ========================================
# SEGURANÇA JWT
# ========================================
# URL do serviço de autenticação (obrigatório em produção)
AUTH_SERVICE_URL=http://localhost:8080

# Habilitar/desabilitar interceptor de segurança
# true = segurança habilitada (requer token JWT)
# false = segurança desabilitada (desenvolvimento)
SECURITY_INTERCEPTOR_ENABLED=false

# ========================================
# NOTAS IMPORTANTES
# ========================================
# 1. NUNCA commite o arquivo .env com credenciais reais
# 2. Use valores diferentes para cada ambiente (dev, staging, prod)
# 3. Em produção, use variáveis de ambiente do sistema ou secrets manager
# 4. Mantenha este arquivo .env.example atualizado como referência
